import { useSelector } from "react-redux";

const Navbar = () => {
    return (
        <>
        <nav>
            <div className="nav-center">Navbar</div>
            </nav>
        </>
    )
}
export default Navbar;